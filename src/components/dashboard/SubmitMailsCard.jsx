import{useState}from"react";import{supabase}from"@/lib/supabaseClient";import{useQuery,useQueryClient}from"@tanstack/react-query";import{Button}from"@/components/ui/button";import{Textarea}from"@/components/ui/textarea";import{Badge}from"@/components/ui/badge";import{Send,Loader2,Clock,CheckCircle,XCircle,Mail}from"lucide-react";import{motion}from"framer-motion";import{toast}from"sonner";import{Link}from"react-router-dom";
const sc={pending:{l:"Pending",i:Clock,c:"bg-yellow-500/20 text-yellow-400 border-yellow-500/30"},approved:{l:"Approved",i:CheckCircle,c:"bg-green-500/20 text-green-400 border-green-500/30"},rejected:{l:"Rejected",i:XCircle,c:"bg-red-500/20 text-red-400 border-red-500/30"}};
export default function SubmitMailsCard({userEmail,onSubmitted,submissionsOpen}){const[mails,setMails]=useState("");const[busy,setBusy]=useState(false);const qc=useQueryClient();const{data:reserved=[]}=useQuery({queryKey:["reserved",userEmail],queryFn:async()=>{const{data}=await supabase.from("reserved_usernames").select("*").eq("user_email",userEmail);return data||[];},enabled:!!userEmail});const{data:subs=[]}=useQuery({queryKey:["subs-card",userEmail],queryFn:async()=>{const{data}=await supabase.from("gmail_submissions").select("*").eq("submitted_by",userEmail).order("created_at",{ascending:false});return data||[];},enabled:!!userEmail});
const submit=async()=>{if(!submissionsOpen){toast.error("Closed.");return;}const lines=mails.split("\n").map(l=>l.trim().toLowerCase()).filter(Boolean);if(!lines.length){toast.error("Paste emails.");return;}if(!reserved.length){toast.error("Generate usernames first.");return;}const valid=new Set(reserved.map(r=>r.gmail_address.toLowerCase()));const already=new Set(subs.map(s=>s.email_address.toLowerCase()));const errs=[],ok=[];for(const e of lines){if(!valid.has(e))errs.push(e+" not generated");else if(already.has(e))errs.push(e+" already submitted");else ok.push(e);}const uniq=[...new Set(ok)];if(errs.length){toast.error(errs[0]);return;}if(!uniq.length){toast.error("No valid emails.");return;}setBusy(true);await supabase.from("gmail_submissions").insert(uniq.map(e=>({email_address:e,submitted_by:userEmail,status:"pending"})));toast.success(`${uniq.length} submitted!`);setMails("");setBusy(false);qc.invalidateQueries({queryKey:["subs-card",userEmail]});qc.invalidateQueries({queryKey:["my-submissions"]});onSubmitted?.();};
return<motion.div initial={{opacity:0,y:20}}animate={{opacity:1,y:0}}transition={{delay:0.2}}className="bg-card border border-border rounded-xl p-5 md:p-6"><div className="silver-gradient rounded-lg py-2 px-4 mb-4 text-center"><h3 className="font-orbitron text-sm font-bold text-black uppercase">Submit Mails</h3></div>{!submissionsOpen&&<div className="bg-red-500/10 border border-red-500/30 rounded-lg p-3 mb-4 text-center"><p className="text-red-400 text-sm">Submissions closed</p></div>}{!reserved.length&&<div className="bg-yellow-500/10 border border-yellow-500/30 rounded-lg p-3 mb-4 text-center"><p className="text-yellow-400 text-sm">Generate usernames first</p></div>}<Textarea placeholder="Paste Gmails (one per line)"value={mails}onChange={e=>setMails(e.target.value)}className="bg-secondary border-border min-h-[120px] text-sm mb-4"disabled={!submissionsOpen||!reserved.length}/><Button onClick={submit}disabled={busy||!submissionsOpen||!reserved.length}className="w-full bg-primary text-primary-foreground font-semibold mb-6">{busy?<Loader2 className="w-4 h-4 animate-spin mr-2"/>:<Send className="w-4 h-4 mr-2"/>}{busy?"Submitting...":"Submit"}</Button>{subs.length>0?<div><div className="flex items-center justify-between mb-3"><p className="text-xs font-semibold text-muted-foreground uppercase">Submissions</p><Link to="/Submissions"className="text-xs text-primary hover:underline">View all</Link></div><div className="space-y-2 max-h-64 overflow-y-auto">{subs.slice(0,20).map(s=>{const cfg=sc[s.status]||sc.pending;const I=cfg.i;return<div key={s.id}className="flex items-center justify-between gap-3 bg-secondary/50 rounded-lg px-3 py-2"><div className="flex items-center gap-2 min-w-0"><I className={`w-4 h-4 ${s.status==="approved"?"text-green-400":s.status==="rejected"?"text-red-400":"text-yellow-400"}`}/><span className="text-xs font-mono truncate">{s.email_address}</span></div><Badge variant="outline"className={`${cfg.c} border text-xs`}>{cfg.l}</Badge></div>;})}</div></div>:<div className="text-center py-6 text-muted-foreground"><Mail className="w-8 h-8 mx-auto mb-2 opacity-30"/><p className="text-xs">No submissions</p></div>}</motion.div>;}